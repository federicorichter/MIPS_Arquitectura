LUI $1, 5            # LUI (load immediate) instruction to $1

# Load the second LUIteral (value 10) into register $2
LUI $2, 10           # LUI (load immediate) instruction to $2

# Add the values in $1 and $2, store the result in $3
ADDU $3, $1, $2      # add instruction: $3 = $1 + $2